package com.bee.client.auth;

import java.util.logging.Logger;

public class ToRegisterFragmentEvent {
    private static final Logger logger = Logger.getLogger(ToRegisterFragmentEvent.class.getName());
}
